# Backend

This directory is the `example.com/hydro-gate-monitor-service` Go module. The executable entry point is `main.go` in this directory; it serves the embedded `web` assets and the HTTP API.

```bash
go test ./...
go build ./...
go run .
```
